# VaultSpace

Multi-tenant team workspace with enterprise-grade permissions (RBAC + ABAC + ReBAC).

> Full README coming after implementation is complete.

## Quick Start

```bash
npm install
docker compose up -d
npm run db:push
npm run db:seed
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
